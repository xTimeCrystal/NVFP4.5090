Install [gn-kernels](https://github.com/gau-nernst/gn-kernels), and replace `gn_kernels/csrc/sm120a/cutlass_mm_fp4.cu` with the version in this repo, then `cd ~/gn-kernels` and `pip install -e . --no-build-isolation`. Example NVFP4 MLP impl gets ~10% e2e speed up for transformer training loop.
